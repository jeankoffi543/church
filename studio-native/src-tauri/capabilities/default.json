{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability for the main window (core APIs only for now).",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
